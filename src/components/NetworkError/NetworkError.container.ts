import { connect } from 'react-redux'
import NetworkError from './NetworkError'

const mapState = () => ({
})

const mapDispatch = () => ({
})

export default connect(mapState, mapDispatch)(NetworkError)